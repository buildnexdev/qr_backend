# Copy to .env. Gmail: App password (Account → Security → 2-Step → App passwords).
# Debug: npm run test:smtp

EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=buildnexdev@gmail.com
EMAIL_PASS=rjqdhbtqnirdhvyf
EMAIL_FROM_NAME=Namma QR
# Must be your Gmail or a Gmail-verified "Send mail as" address, or Gmail may reject / rewrite.
EMAIL_FROM_ADDRESS=buildnexdev@gmail.com

# Optional: where registration notifications are delivered (default: buildnexdev@gmail.com)
# REGISTRATION_EMAIL_TO=buildnexdev@gmail.com

# Legacy (still supported if EMAIL_* unset):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
